
/*
// NOTA: Este código é para uma Supabase Edge Function (Deno).
// Ele foi comentado para não quebrar o build do frontend (React/Vite).
// IMPORTANTE: O bundler pode tentar resolver URLs se estiverem ativos. Mantenha comentado.

// import { serve } from "https://deno.land/std@0.168.0/http/server.ts"
// import Stripe from 'https://esm.sh/stripe@12.0.0'

// Fix: Declare Deno to avoid TypeScript errors in environments without Deno types
// declare const Deno: any;

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

// serve(async (req) => { ... código da função ... })
*/
export {}
